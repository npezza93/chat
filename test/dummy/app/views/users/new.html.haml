= link_to sign_in_path, class: "link-back" do
  = material_icon.arrow_back

.container
  %h1.mdc-theme--text-primary-on-primary= t('.title')
  = form_for @user do |f|
    = render 'clearance/users/form', f: f

    %button.mdc-button.mdc-button--primary{ type: :submit } Sign Up
